# Cohere Backend

This directory is bundled with WhisperNotes and contains the local Python backend
used by the Cohere Transcribe engine.

The app runs `cohere_transcribe.py` with the Python executable configured in
Settings. Run `setup_cohere_backend.sh` once to create a local virtual
environment under `~/Library/Application Support/WhisperNotes/CohereBackend`.
