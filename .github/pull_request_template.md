## What does this PR do?

Brief description of the change.

## Why?

Context for why this change is needed.

## How to test

1. Build: `swift build`
2. Run tests: `swift test`
3. Manual steps to verify (if applicable):
   - ...

## Checklist

- [ ] `swift build` succeeds with no warnings
- [ ] `swift test` passes (all tests green)
- [ ] `make lint` passes (no SwiftLint violations)
- [ ] Tested manually on macOS 14+
- [ ] Updated documentation (if behavior changed)
